crab_cols <- function(crabs){
  col <- colnames(crabs)[1] = "crab_color" %>% 
    colnames(crabs)[2] = "gender"
    return(col)
}

This function will rename the column name "color" to "crab_color" and "sex" to "gender".